# 統合研究コンテキスト

更新日: 2026-08-07  
移行元: ChatGPT クラウドプロジェクト「研究」  
ワークスペース: `C:\研究`  
現在の研究プロジェクト: `C:\研究\projects\chaos-sync-thesis`  
LaTeX作業領域: `C:\研究\latex`

## 1. 最上位目的

> 神経系に見られる動的同期に着想を得て、カオス同期を情報圧縮へ応用できるか検討する。

研究動機は、人間の脳が小規模・低消費電力で記憶・統合・想起を実現していることです。ただし、「その原因がカオス同期である」という点は未証明の着想であり、研究発表では断定しません。

## 2. 中心的な研究質問

1. 入力ごとに異なる同期パターンまたは同期多様体を形成できるか。
2. 同期によって消える冗長自由度と、残る入力依存自由度を分離できるか。
3. 同期軌道から、入力・クラス・個体差を識別または復号できるか。
4. 同期表現は入力より実質的に低次元か。
5. 非カオス振動子、静的特徴量、通常の AE/VAE より有利な条件があるか。

研究の中心命題は次のとおりです。

> 同期が自由度を減らすことと、必要な情報を保存する圧縮は同じではない。カオス同期による縮約の中に入力依存情報を残し、それを Graph–TM–Koopman 表現から安定に復号できる条件を明らかにする。

## 3. 事実・仮説・未確定事項の区別

### 比較的確立している出発点

- 結合カオス系では、条件によって完全同期・一般化同期・クラスタ同期が生じる。
- 同期多様体の局所安定性は、横断 Lyapunov 指数や条件付き Lyapunov 指数で評価できる。
- 一般化 Boole 写像には Cauchy 型の不変測度を持つ可解な領域がある。
- Cauchy 座標は Cayley 変換によって単位円上の複素モードへ写せる。
- グラフフーリエ変換は、ネットワーク上の低周波な全体同期と高周波な局所不一致を分ける候補になる。

### 検証すべき仮説

- `lambda_perp < 0` かつ `lambda_parallel > 0` の領域で、冗長性の収縮と動的情報の保持が両立する。
- 同期臨界点直後で、圧縮率・復号可能性・頑健性の均衡が良くなる。
- TM 係数、グラフスペクトル、時間遅延相関を統合すると、同期軌道に残る入力依存情報を読める。
- 完全同期の深部より、クラスタ同期・相対位相・入力依存の同期多様体・有限時間過渡軌道の方が情報保持に適する。

### 現時点で断定しないこと

- カオス同期が脳の省エネルギー性や長期記憶の原因であること。
- 自由度縮約だけで有用な情報圧縮が成立すること。
- TM 係数が画像の意味を自動的に説明すること。
- カオス同期エンコーダが VAE より省電力・高速・高精度であること。
- 無限次元モデルが有限次元の最小モデルより本質的に優れていること。

## 4. 前提理論

### 4.1 カオスと同期

離散力学系を

\[
x_{t+1}=F(x_t)
\]

とします。複数ノードの完全同期多様体は

\[
\mathcal S=\{x_1=x_2=\cdots=x_N\}
\]

です。本研究では完全同期だけでなく、比例同期、位相差同期、クラスタ同期、入力依存同期多様体も対象にします。

候補となる力学的条件は

\[
\lambda_\perp<0,\qquad \lambda_\parallel>0
\]

です。前者はノード間のずれの収縮、後者は同期多様体内の非自明なカオスの維持を表します。ただし、この条件は入力が復元できることを保証しません。

### 4.2 一般化 Boole 写像と Cauchy 測度

局所写像の候補は

\[
f(x)=\alpha x-\frac{\beta}{x}
\]

です。Cauchy 密度を

\[
\rho_\gamma(x)=\frac{\gamma}{\pi(x^2+\gamma^2)}
\]

とすると、パラメータ領域によって尺度母数を解析的に扱える可能性があります。ランダム結合大自由度極限の理論は、同期による自由度縮約の出発点として使います。

### 4.3 TM 基底と Cayley 変換

実数直線を単位円へ写す Cayley 変換を

\[
q_\gamma(x)=\frac{x-i\gamma}{x+i\gamma}
\]

とし、簡略化した TM モード候補を

\[
M_k^{(\gamma)}(x)=q_\gamma(x)^k
\]

とします。軌道または経験測度 `rho_x` に対する係数

\[
c_k(x)=\int_{\mathbb R}\overline{M_k^{(\gamma)}(u)}\,\rho_x(u)\,du
\]

を読み出し特徴とします。

重要な制約は、すべての入力が同一の長時間不変測度へ収束する場合、これらの長時間平均は入力に依存しなくなることです。TM 基底は情報を作るのではなく、既に残っている差を表現します。

### 4.4 グラフフーリエ変換

グラフ Laplacian を

\[
L=V\Lambda V^\top
\]

とし、ノード `i` の TM 係数を `c_ik` とすると、

\[
\widetilde c_{\ell k}=\sum_{i=1}^{N}V_{i\ell}c_{ik}
\]

により、状態・時間モードとノード間の空間モードを統合できます。

### 4.5 AE/VAE との関係

最初の段階では、

\[
x\rightarrow\text{カオス同期系}\rightarrow h_{\mathrm{TM-GFT}}(x)\rightarrow D(h)\rightarrow\hat x
\]

という決定論的オートエンコーダとして評価します。

VAE に拡張する場合は、

\[
h_{\mathrm{TM-GFT}}(x)\rightarrow (\mu_\phi(x),\log\sigma_\phi^2(x))
\]

を追加し、近似事後分布を出力する必要があります。カオス同期系が決定論的ベクトルだけを出す段階では、VAE ではありません。

## 5. 提案するエンコーダ・デコーダ構想

### 5.1 全体像

\[
x
\xrightarrow{\Phi}
\text{初期状態・外力・結合パラメータ}
\xrightarrow{F^T}
\text{同期軌道}
\xrightarrow{\Pi_{\mathrm{TM-GFT-delay}}}
z(x)
\xrightarrow{D}
\hat x
\]

`Phi` が入力をどこへ埋め込むかは未確定です。候補は、初期値、結合重み、Cauchy 尺度母数、同期クラスタ、低速適応変数です。長期記憶を初期値だけに置くと、混合や正の Lyapunov 指数により情報を失いやすいため、入力依存の不変測度・同期多様体・遅い結合変数へ保存する方針を優先します。

### 5.2 潜在表現

候補は

\[
z(x)=\left\{\widetilde c_{\ell k,\tau}(x)\right\}_{\ell\le L_0,\ |k|\le K_0,\ \tau\le T_0}
\]

です。

- `ell`: グラフ周波数。同期クラスタや局所不一致を表す。
- `k`: TM 次数。Cauchy カオスの複素モードを表す。
- `tau`: 時間遅延。過渡・遷移・時間依存情報を表す。

### 5.3 情報を保存する候補

1. 完全同期ではなく同期クラスタの分割パターン
2. ノード間の相対位相・時間遅れ
3. 同期臨界点近傍の有限時間軌道
4. 入力ごとに異なる同期多様体・アトラクタ
5. 入力依存の Cauchy 尺度母数または複素極
6. 状態より遅く変化する結合・適応変数

## 6. 先行研究の役割分担

### 無限次元ランダム結合 Boole 系

同期臨界、Cauchy 安定性、大自由度縮約の理論的出発点です。入力符号化・復号可能性までは扱っていないため、本研究の独自部分と区別します。

### Artificial Kuramoto Oscillatory Neurons

同期型ニューロンを機械学習の特徴結合へ利用できる先行例です。ただし Kuramoto 型同期であり、カオス同期エンコーダの情報保存・復号を直接扱うものではありません。

### TM–Koopman 関連論文

Cauchy 不変測度、Cayley 変換、TM 基底、作用素モードの候補を与えます。質量ギャップ、KMS、量子場の主張は今回の卒論の中心にしません。論文中の広い主張を独立に検証せず採用しません。

### Autoencoder / VAE

圧縮・復元および確率的潜在表現の比較基準です。カオス同期表現が本当に必要かを確認するため、PCA、通常 AE、VAE、非カオス振動子を比較対象にします。

## 7. 実験計画

### E0: 数学・実装の整合性確認

- 一般化 Boole 写像の不変測度を数値ヒストグラムで再現する。
- Lyapunov 指数と同期臨界を既知条件と照合する。
- TM 係数を解析値または高精度数値積分と照合する。
- 日本語版論文と英語版論文の式差を確認する。

成功条件: 実装誤差を切り分けられ、原典と再現計算の対応が明示される。

2026-08-06実行結果: `alpha = 0.4, 0.5, 0.6`、各5 seed、各100,000観測点で、Cauchy尺度、Lyapunov指数、KS距離、簡略Cayleyモードの全事前ゲートを通過した。独立検証37項目も通過した。これは単一非結合ノードの可解性実装を支持するが、結合系や情報圧縮の有用性は未検証である。記録は [E0実験README](../experiments/runs/20260806_E0_boole-invariant-measure/README.md) を参照する。

### E1: 理想同期軌道からの復号

- 既知のクラスタ、位相差、Cauchy パラメータを持つ人工軌道を作る。
- TM、GFT、時間遅延特徴から既知パラメータを復元する。

成功条件: 読み出し側だけを独立に検証し、識別可能条件と失敗条件を示す。

### E2: 頑健性

- 観測ノイズ、欠損、有限観測長、初期値変動に対する復号誤差を測る。
- 同期臨界からの距離と保持時間の関係を測る。

成功条件: どのノイズ・観測長で情報が失われるかを定量化する。

### E3: 結合 Boole 系

- 入力を初期値、結合、尺度母数、クラスタ構造の各方法で注入する。
- `lambda_perp < 0`、`lambda_parallel > 0` と復号可能性の関係を評価する。
- 非カオス系、非結合系、完全同期の深部と比較する。
- E3A で固定結合を走査し、E3B でスカラー結合強度 `K`、E3C で制約付き結合行列 `W` を学習する。
- 状態時刻 `t` と学習ステップ `n` を分け、軌道内では結合を固定し、軌道間で更新する。

成功条件: 「同期した」だけではなく、入力差がどこに残り、どこから読めるかを示す。

### E4: 連想記憶

- 破損入力から記憶アトラクタへ収束できるかを調べる。
- クラス保持、個体差保持、遷移確率、想起精度を分けて評価する。

### E5: MNIST / AE / VAE 拡張

- 決定論的 AE として画像再構成・クラス保持を評価する。
- 必要なら確率的ヘッドを追加して VAE と比較する。

卒論の最小スコープは E0〜E3 です。E4〜E5 は、基礎理論と最小モデルが成立した後の拡張です。

## 8. 評価指標

- 同期: 同期誤差、秩序変数、横断 Lyapunov 指数
- 識別: 分類精度、ARI、NMI、クラス条件付き相互情報量
- 復号: パラメータ推定誤差、MSE、SSIM
- 圧縮: 潜在次元、量子化後の符号長、rate–distortion
- 頑健性: ノイズ強度・欠損率・観測長に対する性能曲線
- 分布: MMD、Wasserstein 距離、自己相関時間、有効サンプル数
- カオスの必要性: 非カオス系・非結合系・静的特徴量とのアブレーション

連続決定論的表現では相互情報量が特異になる場合があるため、有限分解能・量子化・観測ノイズを明示して評価します。

## 9. 重要な数式上の注意

日本語版「ランダム結合系の大自由度極限でのカオス同期現象」の式(25)は、直後の臨界値と整合しない係数を含む可能性があります。英語版との照合では、対応する条件付き Lyapunov 指数は

\[
\lambda_c=
2\log\left[
\frac1{\sqrt2}+\sqrt{\frac12-\langle|\varepsilon|\rangle}
\right]
\]

すなわち

\[
\lambda_c=
\log\left[
1-\langle|\varepsilon|\rangle+\sqrt{1-2\langle|\varepsilon|\rangle}
\right]
\]

と読む方が、臨界値 `sqrt(2)-1` と整合します。卒論で使用する前に、英語版原典と数値計算で再確認します。

## 10. 未解決点・曖昧点

### 研究目的

- 保存・復元したい情報は、画像全体、クラス、両方のどれか。
- 可逆圧縮か、意味を残す非可逆圧縮か。
- 卒論の主成果を定理、解析手法、最小モデル、生成性能のどこに置くか。

### 入力符号化

- 入力を初期状態、外力、結合、尺度母数、同期クラスタのどこへ埋め込むか。
- 入力除去後も記憶を残す必要があるか。
- 教師データ全体を低速結合へ保存する学習則を導入するか。

### 同期

- 完全同期、一般化同期、比例同期、位相差同期、クラスタ同期のどれを主対象にするか。
- 有限 `N` で同期多様体が不変になる結合行列条件は何か。
- 局所安定性だけでなく、吸引領域や riddled basin をどう評価するか。
- 同期臨界点の有限サイズ効果をどう扱うか。

### 読み出し

- TM 基底を適用できる不変測度・関数空間の正確な条件は何か。
- 有限個の TM・グラフモードで識別可能性を保証できるか。
- 長時間平均と有限時間過渡のどちらが情報を保持するか。
- 基底の位相・順序・回転自由度をどう固定するか。

### 復号・生成

- 潜在表現から画像へのデコーダを線形、カーネル、ニューラルネットのどれにするか。
- デコーダの表現力が強すぎて、同期表現の寄与を隠さないか。
- 一つの長い決定論的軌道から得るサンプルの相関をどう補正するか。
- デジタル有限精度で軌道が周期化する影響をどう測るか。

### 圧縮の定義

- 状態次元、アトラクタ次元、KS エントロピー、正の Lyapunov 指数、符号長のどれを主指標にするか。
- 連続表現の相互情報量を、どの量子化・ノイズモデルで有限化するか。
- 入力全体の再構成と、意味的不変表現の両立をどこまで要求するか。

### 理論と実装

- 無限次元極限を卒論の主対象にする必要があるか。
- 可解性を保ったまま学習可能な相互作用を導入できるか。
- カオス方向の勾配爆発と収縮方向の勾配消失をどう扱うか。
- 発表で必要な定理と、数値仮説の境界をどこに置くか。

## 11. 直近の推奨作業

1. 完了: E0 の再現ノートで一般化 Boole 写像・不変測度・Lyapunov 指数・簡略Cayleyモードを確認した。
2. 次: E1A の人工カオス源と線形混合で、TM・時間遅延読み出しが既知パラメータまたは源信号を回収できるか検証する。
3. 「入力画像全体」「クラス」「両方」のどれを卒論の第一目標にするか教授と確定する。
4. 発表では、背景→目的→定義→先行研究→未解決点→仮説→提案方法→実験計画の順を採用する。

## 12. 2026-08-06 発表フィードバックの反映

- 最初の読み出し実験を、人工カオス源の線形混合に対する E1A とする。
- TM 基底は、失われた情報の復元器ではなく、軌道に残る差の候補座標系として評価する。
- 一般化 Boole 写像と Boolean 状態を区別し、`x_i(t)` は実数値として扱う。
- `tanh` と Cauchy 分布の関係は、対象変数と原典が未確定のため採用しない。
- 自由エネルギーは、条件付き目標分布、情報保持項、同期項、符号長を定義した後の比較候補とする。
- Cauchy 初期化、非定常初期化、外部入力を区別する。
- エントロピー単独最小化は避け、復号誤差と量子化後符号長の rate-distortion を中心にする。
- 結合学習は読み出しのみ、学習スカラー `K`、制約付き `W` の順に比較し、学習後は凍結評価する。
- 詳細は [PRESENTATION_FEEDBACK_2026-08-06.md](PRESENTATION_FEEDBACK_2026-08-06.md) に記録する。
- 学習設計の詳細は [MODEL_TRAINING_DESIGN.md](MODEL_TRAINING_DESIGN.md) に記録する。
