# {{TITLE}}

- 実験 ID: `{{EXPERIMENT_ID}}`
- 段階: `{{STAGE}}`
- 状態: `planned`
- 作成日時: `{{CREATED_AT}}`
- seed: `{{SEED}}`
- 系列（現行教材 / 旧系列 / その他）:
- 成果物の相対パス（同じ実験IDの別実行を区別）:
- 親実行のパス・親から変えた要因:
- 実行状態 / 整合性検証 / 科学的判断: `planned / not_checked / not_evaluated`

## 研究質問

何を判定する実験かを書く。

## 仮説

実験前に、期待する結果とその理由を書く。

## 反証条件

どの結果なら仮説を支持しないと判断するかを書く。

## データ

- 生成元または取得元:
- 入力と目的変数:
- 軌道数・観測長:
- burn-in:
- train/validation/test の分割単位:
- Cauchy サンプリングを使う箇所と理由:

## データと成果物の対応

パスはこのレポートからの相対パスで記入する。未保存・該当なしは理由を記す。

| 段階 | 実在ファイル | 配列名・形状・行キー / 由来 |
| --- | --- | --- |
| 実行時設定 | | seed、RNG、初期値、split、精度、観測長 |
| 入力データ | | 生データか標準化済みか、親実行との共有範囲 |
| 前処理・特徴 | | 条件表との行対応、前処理統計のfit対象 |
| 選択・凍結モデル | | 候補評価とtest前確定の記録 |
| 個別予測 | | 予測と真値を結ぶキー |
| 条件別・seed別指標 | | 集約方法、区間の差の向き・標本単位 |
| 図・表 | | 元CSV/NPZ、抽出条件、生成スクリプト/関数 |
| 実行時ソース・環境 | | ZIPまたはcommit＋差分、依存版・thread |
| ハッシュ・検証 | | 対象範囲と除外、検証結果のパス |

- Git追跡 / 外部保存 / ローカルのみ:
- 未保存データの再生成方法・既知の不足:

## 方法

- 前処理:
- モデルまたは写像:
- TM 基底の定義と尺度:
- 時間遅延:
- 学習方法:

## 学習設計

- 学習するパラメータ:
- 固定するパラメータ:
- 状態時間 `t` と学習ステップ `n` の更新順:
- 損失項と各重み:
- 結合行列の制約:
- optimizer、勾配 clipping、停止条件:
- test 時に凍結する対象:

## 比較対象とアブレーション

- ベースライン:
- 除去する構成要素:

## 評価

- 主指標:
- 副指標:
- seed 間の集約方法:
- 成功条件:

## 実行

```powershell
# 実際に使用したコマンドを記録する。
```

## 結果

未実行。数値、図表への相対パス、失敗を含む観察を記録する。

## 解釈

結果から言えること、言えないこと、代替説明を書く。

## 判断

`adopt`、`revise`、`reject`、`inconclusive` のいずれかと理由を書く。

## 次の一手

次に変更する一要因と、参照する後継実験 ID を書く。

## 記録完了確認

- 結果・データ対応台帳 `experiments/RESULTS_INDEX.md` の該当行:
- 検証コマンドと実行結果（保存済み記録の参照か、今回実行かを区別）:
- 図表・データのリンクと配列/行対応の確認:
- 仮説不支持・未実行項目・保存不足を残したか:
